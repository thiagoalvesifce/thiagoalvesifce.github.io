double x = x + x

quadruple x = double (double x)

fat n = product [1..n]

media ns = (sum ns) `div` (length ns)

main = do n <- readLn
	  print (fat n)
	  nome <- getLine
	  putStr ("Hello, " ++ nome ++ "!")
